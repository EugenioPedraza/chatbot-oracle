version: 0.1 
component: build
timeoutInSeconds: 600
shell: bash

env:
  variables:
    JAVA_HOME: /usr/lib64/graalvm/graalvm22-ee-java17

  exportedVariables:
    - BuildServiceDemoVersion

steps:
  - type: Command
    name: "Install GraalVM Enterprise 22.x Native Image for Java17"
    command: yum -y install graalvm22-ee-17-native-image

  - type: Command
    name: "Set PATH Variable"
    command: export PATH=$JAVA_HOME/bin:$PATH
    
  - type: Command
    name: "Docker Login"
    command: |
      cd MtdrSpring;
      oci os object get --bucket-name reacttodo-ehtx3 --name deployment_config.tgz --file deployment_config.tgz || exit 1
      tar -xzvf deployment_config.tgz || exit 1
      source env.sh || exit 1
      cat at.cfg | docker login -u "axjjcwgmt6tj/a00834105@tec.mx" --password-stdin mx-queretaro-1.ocir.io || exit 1


  - type: Command
    name: "Build"
    command:  cd MtdrSpring;
              source env.sh;
              (cd backend;source build.sh)



